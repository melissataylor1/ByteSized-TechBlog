<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Byte Sized</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>
  <header>
    <h1>Byte Sized Tech Blog</h1>
    <nav>
      <ul>
        <li><a href="/" class="homepage">
            <div>Home</div>
          </a></li>
        <li>
          <div id="dashboard">
            {{#if loggedIn}}
            <a href="/dashboard">Dashboard</a>
            {{else}}
            <a href="/login">Dashboard</a>
            {{/if}}
          </div>
        </li>
        <li>
          <div id="logoutBtn">
            {{#if loggedIn}}
            <button type="button" id="logout-Btn">Logout</button>
            {{else}}
            <a href="/login" class="loginBtn">Login/Sign Up</a>
            {{/if}}
          </div>
        </li>
      </ul>
    </nav>
  </header>

  <main>
    {{{body}}}
  </main>

  <script src="/js/logout.js"></script>
</body>

</html>